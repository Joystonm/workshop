// Physics workshop - virtual physics laboratory.
// The actual UI lives in <LabShell /> which mounts a deterministic
// simulation engine and a set of fully-interactive experiments.

import { LabShell } from '../../components/physics/LabShell'

export function PhysicsWorkshop() {
  return <LabShell />
}
